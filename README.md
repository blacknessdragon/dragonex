# dragonex
this version is a usable base for app like students attendens app like generates QR code that give you url .
dynamically created widgets and removes when you left the screen ,
the old version tryies to use UDP in base connection it had been solved by using fastapi  framework .
# up-scale
in database
in upscaling the  app you can use mysql ,alexandra,supabase or mangodb according to papears it should work.
in web application 
you can use django or .NET , ...etc .

